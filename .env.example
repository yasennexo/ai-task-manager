# Number of days back to look when extracting tasks from Slack/Gmail.
# Lower values = faster extraction. Default: 7
# LOOKBACK_DAYS=3

# Slack bot token for the morning reminder script.
# Create one at https://api.slack.com/apps → New App → OAuth & Permissions
# Add scope: chat:write (bot) or chat:write (user token xoxp-)
# SLACK_BOT_TOKEN=xoxb-...
